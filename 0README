Repository for developing scipts for optimization of protein-protein interactions.

// Example commit
git add *.R
git commit -a -m "adding R scripts for optimizing energies and distances simultaneously, and with different potential functions"
git push

// Example to merge master with other branch
git checkout master
git merge dev1
git push

